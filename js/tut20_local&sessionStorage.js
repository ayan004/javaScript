console.log('We are in tut20');

//There are two types of storages available in client-side browser, that can be used by javaScript
//1. Local storage- Local storage stores the data for all the sessions, i.e. if you close the browser then also the data will stay.
//2. Session storage- Session storage stores the data for a particular session, i.e. if you close the browser window then the data wll get erase.
//Mostly all functions are same for local storage and session storage.

let Array=  ["Apple","Orange","Mango"];
//console.log(Array);

// add a key value pair inside local storage
// localStorage.setItem(`start`, `eight`);
// localStorage.setItem(`cont`, `nine`);
// localStorage.setItem(`end`, `10and1/2`);
// localStorage.setItem(`fruits`, JSON.stringify(Array));

// clears the entire local storage
// localStorage.clear();

// clear a particular key-value pair
// localStorage.removeItem(`cont`);

// let variable= JSON.parse(localStorage.getItem(`fruits`));
// console.log(variable);

// sessionStorage.setItem(`start`, `eight`);
// sessionStorage.setItem(`cont`, `nine`);
// sessionStorage.setItem(`end`, `10and1/2`);
// sessionStorage.setItem(`fruits`, JSON.stringify(Array));

//let variable= sessionStorage.getItem(`start`);
//console.log(variable);


